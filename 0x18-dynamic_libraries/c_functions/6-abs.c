#include "main.h"

/**
 * _abs - function to find absolute value
 * @r: character input for function
 *
 * Return: r if >=0,
 *        -r if <0
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
