#include <stdio.h>

/**
 * main - function that prints name
 * @argc: Argument Count
 * @argv: Array of pointers to the Argument
 * Return: 1
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
