#include <stdio.h>

/**
 * main - main function
 *
 * Description: Hexadecimal
 *
 * Return: 0 (success)
 */
int main(void)
{
	char a, b;

	for (a = 48; a <= 57; a++)
	{
		putchar(a);
	}

	for (ch = 97; ch <= 102; ch++)
	{
		putchar(ch);
	}
	putchar('\n');

	return (0);
}
